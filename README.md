# Generador de PDFs con PhantomJS, ES6

## Setup

Crear carpeta llamada `tmp` en el root del proyecto, aca se almacenaran los PDF generados


Instalar dependencias

`yarn install`


Ejecutar el servicio

`yarn start`